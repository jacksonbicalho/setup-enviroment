#!/bin/bash

# set -e

# token=""
# now=$(date)
# key_pub=""
# curl --data-urlencode "key=$key_pub" --data-urlencode "title=setup-enviroment $now" \
# https://gitlab.com/api/v4/user/keys?private_token=$token

